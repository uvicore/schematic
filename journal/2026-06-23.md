# 2026-06-23

## Dropped `update_forward_refs()` from the database skill
- **Intent:** Pydantic v2 makes per-model `update_forward_refs()` obsolete (framework rebuilds models
  centrally at boot), but the scaffold's skill still told app authors to call it.
- **Changed:** Updated the model example to import forward-ref types at the bottom of the file with
  no rebuild call, and reworded the checklist item to match.
- **Files:** `.claude/skills/uvicore-database/SKILL.md`.
- **Related:** framework [ADR 0003](../../framework/adr/0003-central-model-rebuild.md);
  docs ORM pages updated.

## Added "where knowledge lives" convention + established journal/ADR
- **Changed:** Added the repo-vs-machine-local knowledge note to `CLAUDE.md`; created this `journal/`
  and `adr/` with READMEs/templates.
- **Files:** `CLAUDE.md`, `journal/*`, `adr/README.md`.
