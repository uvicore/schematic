# 2026-06-24

## App stub: new OpenAPI config knobs (framework 0.4.4)

- **Intent:** Keep generated apps in sync with the framework 0.4.4 OpenAPI changes.
- **Changed:** `acme/appstub/config/http.py` — added `api.openapi.separate_schemas`
  (`env.bool('OPENAPI_SEPARATE_SCHEMAS', False)`) and `api.openapi.docs.models_expansion`
  (`env.int('OPENAPI_MODELS_EXPANSION', -1)`) with explanatory comments, so freshly generated apps
  ship leaner Swagger docs by default (one schema per model, Schemas section hidden).
- **Related:** framework journal 2026-06-24.

## Per-operation Swagger knobs (follow-up)

- **Changed:** `acme/appstub/config/http.py` — added `api.openapi.docs.model_expansion`
  (`env.int('OPENAPI_MODEL_EXPANSION', 1)`) and a commented `parameters` passthrough example, so
  generated apps can collapse per-operation model trees / set arbitrary Swagger UI options.
- **Related:** framework journal 2026-06-24.
